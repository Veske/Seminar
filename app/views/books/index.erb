<h1>Raamatud</h1>

<table>
  <tr>
    <th>Pealkiri</th>
    <th>Ilmumise aasta</th>
    <th>Autor</th>
    <th></th>
    <th></th>
  </tr>

  <% @books.each do |book| %>
      <tr>
        <td><%= book.name %></td>
        <td><%= book.published %></td>
        <td><%= book.author %></td>
        <td><%= link_to 'Näita', book %></td>
        <td><%= link_to 'Muuda', edit_book_path(book) %></td>
        <td><%= link_to 'Kustuta', book_path(book), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
</table>

<br>

<%= link_to 'Uue raamatu lisamine', new_book_path %>
